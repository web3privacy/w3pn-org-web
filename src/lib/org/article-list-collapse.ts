/** Visible article cards before "Show all articles" (desktop / tablet). */
export const ARTICLES_COLLAPSE_LIMIT_WIDE = 4;
/** Visible article cards on narrow viewports (mobile). */
export const ARTICLES_COLLAPSE_LIMIT_NARROW = 3;
/** Breakpoint (px) aligned with common mobile layouts in org CSS. */
export const ARTICLES_COLLAPSE_MEDIA = "(max-width: 640px)";
